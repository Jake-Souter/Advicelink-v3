# Marketing site placeholder

Per `REBUILD_PLAN.md` §3 the marketing site lives at `advicelink.org` and is built with Next.js (static export). Implementation deferred until launch marketing content is ready — this folder is reserved so the workspace structure matches the spec and `pnpm` workspace globs resolve.

When the site is needed, swap this `package.json` for a real Next.js app and connect it to its own Vercel project.

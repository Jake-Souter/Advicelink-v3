/**
 * `chartjs-node-canvas` wrappers for the three chart types used in DOCX
 * outputs (REBUILD_PLAN §8.7):
 *   - Asset allocation pie (recommended portfolio)
 *   - Projection line chart (balance vs age)
 *   - Insurance needs vs cover bar chart
 *
 * Renders PNGs at 2x DPI; the same Chart.js config drives the live UI charts
 * for visual parity.
 */
export {};

/**
 * InsuranceQuotePort + OmniLife adapter (Basic Auth, API v4.58 locked).
 *
 * Trial supplier set at v1: TAL (`ACC`), Zurich (`ZUR`). Codes outside the
 * trial set are silently dropped; an empty resolved set falls back to all
 * trial codes. See REBUILD_PLAN.md §19.12.6 and the locked spec at
 * `docs/OmniLife-API-Reference.md` (1,598 lines, v4.58).
 *
 * Populated by Work Package 13.
 */
export {};

/**
 * Zod schemas for every entity, DTO, and wizard section.
 *
 * Populated by Work Package 6 (Fact Find) onwards. Per REBUILD_PLAN §17, every
 * feature ships its Zod schema here before the tRPC procedure or frontend
 * form references it. The frontend imports inferred types via the tRPC
 * client; never duplicate a shape.
 */
export {};

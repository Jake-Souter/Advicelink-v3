/**
 * Insurance needs-analysis engine + risk-profile scoring.
 *
 * Defaults that ship in `insurance-settings/default.json` are documented in
 * REBUILD_PLAN.md §19.6. Tenant overrides go through admin (§10.5); runs
 * pin the version they used. Risk-profile scoring map: §19.7.
 */
export {};
